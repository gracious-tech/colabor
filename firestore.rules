
rules_version = '2';

service cloud.firestore {
    match /databases/{database}/documents {
        match /fundraisers/{fund_id} {
            match /pledges/{pledge_id} {
                allow create;
                allow update;
            }
        }
    }
}
